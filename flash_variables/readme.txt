Flash variable example.

This example project use EFM32 CMSIS, the emlib peripheral library and the
libttdcore utility library to demonstrate the Flash variable operation.

The TDxxxx RF module will increment 2 variables stored in Flash memory, then
reboot automatically. Once the first value reaches a predefined value, all
stored variables will be earased.
